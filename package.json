{
  "name": "gerenciador-de-operacoes",
  "private": true,
  "description": "Front estático (HTML/CSS/JS em ES modules). Sem bundler — não há compilação.",
  "scripts": {
    "start": "npx --yes serve public",
    "build": "node -e \"console.log('Build: site estático já em public/. Nada a compilar.'); process.exit(0);\""
  }
}
